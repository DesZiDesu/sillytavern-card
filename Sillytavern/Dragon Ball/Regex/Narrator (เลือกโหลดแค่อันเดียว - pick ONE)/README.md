# Dragon Ball Narrator — install ONLY ONE

Scene narration, no character speaking. Tag `[DBNARR|text]`. Renders **above** the header. Import one:

| File | Design |
|---|---|
| `DB_Narrator_A_MangaCaption_Regex.json` | Rectangular cream ink caption box (pairs with Dialogue A) |
| `DB_Narrator_B_BattleCaption_Regex.json` | Centered dark caption band (pairs with Dialogue B) |

Separate from **Character Monologue** — narrator has no name/color, it's the story's voice.
